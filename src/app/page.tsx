// Kara owns this file.
// Bacon stub: renders a placeholder so the dev server starts.

export default function Home() {
  return (
    <main style={{ padding: '48px 24px', textAlign: 'center' }}>
      <h1 style={{ color: 'var(--g)', fontSize: '2rem', fontWeight: 800 }}>Serve Sync v2</h1>
      <p style={{ color: '#475569', marginTop: '8px' }}>Kara — drop your components in here.</p>
    </main>
  );
}
